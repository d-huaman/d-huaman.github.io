# d-huaman.github.io

# assets/cv/

Coloca aquí tu CV en PDF con exactamente este nombre (minúsculas, con guiones):

```
cv-daniel-huaman-izquierdo.pdf
```

El botón "Descargar CV" del footer (`index.html`) ya apunta a esta ruta —
no hay que tocar el código, solo agregar el archivo.

Recomendación: que el PDF pese menos de ~2MB para que la descarga sea instantánea.
